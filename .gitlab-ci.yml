---
# You can override the included template(s) by including variable overrides
# SAST customization: https://docs.gitlab.com/user/application_security/sast/#available-cicd-variables
# Secret Detection customization: https://docs.gitlab.com/user/application_security/secret_detection/pipeline/configure/
# Dependency Scanning customization: https://docs.gitlab.com/user/application_security/dependency_scanning/#customizing-analyzer-behavior
# Container Scanning customization: https://docs.gitlab.com/user/application_security/container_scanning/#customizing-analyzer-behavior
# Note that environment variables can be set in several places
# See https://docs.gitlab.com/ci/variables/#cicd-variable-precedence
stages:
  - test
  - secret-detection
  - lint
  - k8s-test

sast:
  stage: test

include:
  - template: Security/SAST.gitlab-ci.yml
  - template: Security/Secret-Detection.gitlab-ci.yml

variables:
  SECRET_DETECTION_ENABLED: 'true'

secret_detection:
  stage: secret-detection

# ──────────────────────────────────────────────
# Ansible Lint
# ──────────────────────────────────────────────
ansible-lint:
  stage: lint
  image: python:3.12-slim
  before_script:
    - pip install --quiet tox
  script:
    - tox -e ansible-lint
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      changes:
        - "**/*.yml"
        - "**/*.yaml"
        - roles/**/*
        - playbooks/**/*

# ──────────────────────────────────────────────
# YAML Lint
# ──────────────────────────────────────────────
yaml-lint:
  stage: lint
  image: python:3.12-slim
  before_script:
    - pip install --quiet tox
  script:
    - tox -e yaml-lint
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      changes:
        - "**/*.yml"
        - "**/*.yaml"
# ──────────────────────────────────────────────
# K8s App-of-Apps Test (KIND + Terraform)
# ──────────────────────────────────────────────
k8s-app-of-apps-test:
  stage: k8s-test
  image:
    name: hashicorp/terraform:1.10
    entrypoint: [""]
  services:
    - name: docker:27-dind
      alias: docker
      variables:
        DOCKER_TLS_CERTDIR: /certs
  variables:
    DOCKER_HOST: tcp://docker:2376
    DOCKER_TLS_CERTDIR: /certs
    DOCKER_TLS_VERIFY: "1"
    DOCKER_CERT_PATH: /certs/client
    CLUSTER_NAME: ci-$CI_PIPELINE_ID
    TF_DIR: $CI_PROJECT_DIR/terraform/kind
  before_script:
    - sh "$CI_PROJECT_DIR/scripts/k8s-app-of-apps-install-deps.sh"
  script:
    - bash "$CI_PROJECT_DIR/scripts/k8s-app-of-apps-test.sh"
  after_script:
    - bash "$CI_PROJECT_DIR/scripts/k8s-app-of-apps-test.sh" destroy_cluster
  artifacts:
    when: always
    paths:
      - artifacts/
    expire_in: 7 days
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      changes:
        - k8s-manifests/**/*
